[project]
name = "namescout"
version = "0.1.0"
dependencies = ["httpx"]
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["namescout*"]

[project.scripts]
namescout = "namescout.cli:main"
